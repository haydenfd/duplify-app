import {
    BACKEND_ENDPOINTS,
    CLIENT_URL,
} from "./ApiConfig";

import {
    validateToken,
} from "./ValidateToken";

import {
    SLIDE_DATA,
} from "./SlideData";

export {
    SLIDE_DATA,
    validateToken,
    BACKEND_ENDPOINTS,
    CLIENT_URL,
}